import { AppPaginatorDirective } from './app-paginator.directive';

describe('AppPaginatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AppPaginatorDirective();
    expect(directive).toBeTruthy();
  });
});
